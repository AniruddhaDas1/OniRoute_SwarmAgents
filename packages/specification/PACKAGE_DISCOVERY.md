# Package Discovery

Packages may be discovered through the OniRoute Registry, registered Knowledge Sources, Community catalogs, Enterprise repositories, local Package locations, and a future marketplace.

Discovery returns metadata, provenance, trust, validation, version, compatibility, dependency, distribution, and installation-eligibility evidence. It does not import, normalize, install, or execute Packages. Duplicate detection uses canonical ID, source, version, and content digest.

