# Package Validation

Validate canonical schema, identity, version, type, lifecycle, timestamps, content inventory, path integrity, licenses, attribution, provenance, immutable revision, integrity, dependency graph, cycles, conflicts, compatibility, trust, size, and prohibited undeclared executable behavior.

Results record Passed, Failed, Quarantined, or Needs Review without installing the Package.

