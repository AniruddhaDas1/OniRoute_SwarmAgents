# Security Policy

Report vulnerabilities privately through the repository security advisory mechanism when available; do not publish exploit details in a public issue. Include affected version, reproduction, impact, and mitigations. Never include live credentials. OniRoute stores no secrets and provides no secrets manager. Automatic AI invocation must be explicitly enabled and governed. Tool/MCP execution is not implemented in v0.6.
