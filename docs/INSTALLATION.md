# Installation

OniRoute requires Python 3.12+, Git, and a local clone. Create a virtual environment and run `python -m pip install -e .`. Verify with `oniroute doctor` and `python -m pytest -q`. No database, server, API key, or internet connection is required for metadata, planning, Dry Run, and validation features. Real invocation requires an explicitly configured compatible endpoint.
