# Features

Discover hundreds of Agent and Skill records; validate the repository; resolve relationships; build immutable Context; plan and run declarative Workflows; select local or remote models through capabilities; inspect Tool/MCP metadata; and enforce approvals, permissions, budgets, risk, and audit records locally.
