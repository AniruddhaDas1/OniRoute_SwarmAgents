# Getting Started

Clone the repository, create a Python 3.12+ virtual environment, install editable dependencies, and run `oniroute doctor`. Explore Workflows with `oniroute list workflows`, build a plan, run in Dry Run mode, and inspect model, Tool, and governance recommendations before configuring a real local endpoint.
