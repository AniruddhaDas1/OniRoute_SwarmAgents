# Architecture

OniRoute separates organizational metadata from execution. Frozen definitions feed a local registry and graph. Resolution and Context prepare deterministic plans. UMAL and the Invocation Layer isolate model providers. The Tool Layer unifies local and MCP metadata. Governance is mandatory at AI and Tool choke points.
