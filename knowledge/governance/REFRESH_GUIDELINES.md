# Refresh Guidelines

Refresh knowledge when source, license, version, contracts, mappings, dependencies, trust, validation evidence, or architecture changes. Review deltas rather than silently replacing records. Maintain decision history and invalidate stale resolver caches.

