# Resolution Fallback Policy

Fallback follows the permanent order without skipping policy gates. Deprecated candidates use declared successors. Disabled, archived, incompatible, or unresolved candidates are excluded. Equal candidates return a deterministic evidence record or review request. If no candidate remains, return Missing Capability and a non-executable Recommendation.

