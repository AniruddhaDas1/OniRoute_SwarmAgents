# Resolution Decision Records

Every decision records request identity, Agent/Sub-Agent, capability, candidates by tier, exclusions, selected knowledge, versions, provenance, trust, validation evidence, confidence, dependencies, fallback use, missing capability, recommendation, policy version, timestamp, and reviewer when applicable. Records are append-only and auditable.

