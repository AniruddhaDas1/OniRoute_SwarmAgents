# Knowledge Resolution

A resolution request combines Agent identity, optional Sub-Agent identity, capability, context contracts, trust requirements, compatibility, lifecycle, and evidence. The resolver reads the external mapping registry, applies the permanent order, filters invalid candidates, scores confidence, expands reviewed dependencies, and returns a traceable decision. Resolution never changes ownership or silently promotes knowledge.

