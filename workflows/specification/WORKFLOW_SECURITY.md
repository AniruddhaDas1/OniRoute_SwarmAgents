# Workflow Security

Every Workflow declares a security classification and handling expectations for inputs, context, artifacts, dependencies, decisions, approvals, retention, and provenance. Classification must be understandable independently of any provider or platform. The specification does not prescribe cryptography, identity systems, networks, or deployment controls.
