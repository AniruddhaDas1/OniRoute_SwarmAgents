# Workflow Versioning

Workflow versions use semantic versioning. Major versions indicate incompatible contract, participant, artifact, context, or dependency changes; minor versions add backward-compatible declarations; patches correct clarifications or metadata without changing the contract. Every update records rationale, author, date, compatibility impact, and migration notes where needed. Version compatibility is distinct from runtime implementation compatibility.
