# Workflow Dependencies

Dependencies are declarative references only. Supported kinds are **Workflow**, **Skill**, **Package**, **Knowledge Source**, and **Artifact**. Each dependency records identifier, required version or range, purpose, optionality, provenance, and validation state. Dependencies describe prerequisites and compatibility; they do not invoke, install, import, resolve, or execute anything at runtime.
