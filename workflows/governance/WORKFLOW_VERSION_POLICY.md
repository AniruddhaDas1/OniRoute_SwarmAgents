# Workflow Version Policy

Semantic versioning governs Workflow contracts. Breaking participant, context, artifact, approval, dependency, or completion changes require a major version; compatible additions require a minor version; corrections require a patch. Every version records rationale, review, compatibility impact, and migration guidance.
