# Workflow Resolution Dependencies

Resolution supports declarative dependencies on **Workflows**, **Skills**, **Packages**, **Knowledge Sources**, and **Artifacts**. Each reference records identity, version/range, purpose, optionality, provenance, compatibility, and validation state. Dependencies are checked for completeness, lifecycle, and cycles; they are never installed, invoked, loaded, or executed.
